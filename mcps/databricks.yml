bundle:
  name: mcp-cyber-server

sync:
  include:
    - .build

artifacts:
  default:
    type: whl
    path: .
    build: uv build --wheel

resources:
  apps:
    mcp-cyber-server:
      name: "mcp-cyber"
      description: "Cyber MCP Server on Databricks Apps"
      source_code_path: ./.build

targets:
  dev:
    mode: development
    default: true
